conda clean -p
conda clean -t
pip cache purge
 sudo du -h xx | sort -h

 ascontiguousarray 函数将一个内存不连续存储的数组转换为内存连续存储的数组
 ONNX(angx)：open neural network exchange

不支持9，改为8
/home/fengsc/anaconda3/envs/py37/lib/python3.7/site-packages/onnx_graphsurgeon/exporters/onnx_exporter.py:181:    model.ir_version=8

onnx_graphsurgeon
